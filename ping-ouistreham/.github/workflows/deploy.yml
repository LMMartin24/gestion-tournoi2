name: Deploy Website

on:
  push:
    branches:
      - main # Le déploiement se lance quand on push sur la branche main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Deploy to Hostinger via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_KEY }}
          port: 65002
          script: |
            cd domains/votre-domaine.com/public_html
            git pull origin main
            # Ajoutez ici des commandes si besoin (ex: npm install ou composer install)